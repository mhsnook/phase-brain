{
  "name": "phase-brain",
  "version": "0.1.0",
  "private": true,
  "description": "Order-parameter-coupled switching network — a no-build oscillator toolbox",
  "type": "module",
  "scripts": {
    "lint": "oxlint js tests",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run lint && npm run typecheck && npm run test"
  },
  "devDependencies": {
    "jsdom": "^24.1.3",
    "oxlint": "^0.15.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
